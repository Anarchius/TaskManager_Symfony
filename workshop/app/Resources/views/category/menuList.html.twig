<p>
    <a href="/category/new" class="btn btn-lg btn-success" >New category</a>
</p>

{% for category in categories %}
    <a href="{{ path('category_show', { 'id': category.id }) }}" class="btn btn-info" style="margin-bottom: 10px;">
        <span class="fa fa-{{ category.icon }}">&nbsp;</span> {{ category.name }}
    </a>
    <br>
{% endfor %}
